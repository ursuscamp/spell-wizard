{
  "name": "spell-wizard",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "db:reset:test-data": "node scripts/reset-and-seed-local-db.mjs",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "seed:rank-edge-profiles": "node scripts/seed-rank-edge-profiles.mjs",
    "test": "npm run build && node --test tests/**/*.test.mjs"
  },
  "dependencies": {
    "@andresaya/edge-tts": "^1.8.0",
    "nuxt": "^4.4.2",
    "vue": "^3.5.30",
    "vue-router": "^5.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.5.0"
  }
}
